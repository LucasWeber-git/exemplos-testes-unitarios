logging:
  level:
    root: DEBUG

server:
  port: 0

spring:
  application:
    name: exemplos-testes-unitarios
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
  main:
    banner-mode: "off"
  rabbitmq:
    connection-timeout: 1
